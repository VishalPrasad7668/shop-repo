{%- if section.settings.title != blank -%}
        <h2>
          {{ section.settings.title }}
        </h2>
      {%- endif -%}
{% schema %}
  {
    "name": "Products Show",
    "settings": [
      {
      "type": "inline_richtext",
      "id": "title",
      "label": "Product Show Title"
    }
    ],
    "presets": [
      {
        "name": "Products Show"
      }
    ]
  }
{% endschema %}